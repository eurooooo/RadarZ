本地运行

1. /backend 文件夹下 创建 .env，按照.env.example 配置环境变量
2. cd backend 然后 uv sync
3. cd frontend 然后 npm run install
4. cd backend -> uv run fastapi dev main.py
5. cd frontend -> npm run dev
6. 访问 localhost:3000
